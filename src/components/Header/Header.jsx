import React from 'react'
import s from './Header.module.scss'

const Header = (props) => {
    return (
        <h1 className={s.headerTitle}>Weather Forecast React App</h1>
    )
}

export default Header